# Link Juice Simulator (SEO Internal Linking Simulator)

Front-only tool (Tailwind CDN + D3 CDN) to simulate a PageRank-like “link juice” and visualize internal linking.

## Run locally
Open `index.html` with a local server (recommended):
- VSCode Live Server
- `python -m http.server 5173`